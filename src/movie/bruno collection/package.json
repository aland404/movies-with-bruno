{
  "name": "movie-bruno-api-collection",
  "version": "1.0.0",
  "dependencies": {
    "@ngneat/falso": "^7.2.0"
  },
  "devDependencies": {
    "@usebruno/cli": "^1.16.0"
  },
  "scripts": {
    "run": "bru run . -r",
    "run:local": "bru run . -r --env localhost",
    "run:collect": "bru run . -r --env localhost --output run-collect-results.json"
  },
  "license": "MIT"
}
