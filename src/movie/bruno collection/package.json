{
  "name": "movie-bruno-api-collection",
  "version": "1.0.0",
  "dependencies": {
    "@ngneat/falso": "^7.2.0"
  },
  "devDependencies": {
    "@usebruno/cli": "^1.34.1"
  },
  "scripts": {
    "run:local": "bru run . -r --env localhost --env-var username=john --env-var password=changeme",
    "run:local:tests-only": "bru run . -r --env localhost --env-var username=john --env-var password=changeme --tests-only",
    "run:collect": "bru run . -r --env localhost --env-var username=john --env-var password=changeme --output run-collect-results.json"
  },
  "license": "MIT"
}
